# 03-i18n — Macrotask `i18n` · Internazionalizzazione IT/ES

> Modulo del blueprint P0 (fondamenta) di Belora. Un modulo = un macrotask:
> l'unita al cui confine gira il checkpoint e l'unita di commit atomico.
> Task atomici secondo lo schema trueline (id/AC/target_tests/security_notes).

## Obiettivo del macrotask

next-intl con routing per locale (/it, /es), cataloghi messaggi con parita di chiavi tra le lingue, selettore lingua con persistenza.

## Task atomici

```yaml
- id: T-080
  title: "Setup next-intl + routing per locale"
  macrotask: "i18n"
  depends_on: [T-001, T-020]
  objective: "Configurare next-intl nel progetto Next.js App Router con routing basato su locale: segmento dinamico [locale] con prefissi /it e /es, locale di default 'it', middleware next-intl in src/middleware.ts, che T-041 estende con la guardia auth senza indebolire il routing di locale, plugin next-intl in next.config, file di request config (getRequestConfig) e wrapping del layout in NextIntlClientProvider con setRequestLocale. Le richieste senza prefisso di locale vengono reindirizzate al locale di default; un locale non supportato produce 404. localePrefix impostato ad 'always' cosi la root reindirizza esplicitamente."
  definition_of_done:
    - "File src/i18n/routing.ts che dichiara locales=['it','es'], defaultLocale='it' e localePrefix='always' (via defineRouting di next-intl)."
    - "File src/i18n/request.ts con getRequestConfig che risolve il locale attivo e carica i messaggi corrispondenti."
    - "File src/middleware.ts che usa createMiddleware(routing) di next-intl con matcher (verra ESTESO dalla guardia auth di T-041 nello stesso file) che esclude /api, /_next e gli asset statici."
    - "next.config con createNextIntlPlugin('./src/i18n/request.ts') applicato all'export."
    - "Directory src/app/[locale]/layout.tsx che avvolge i children in NextIntlClientProvider e chiama setRequestLocale(locale)."
    - "Dipendenza next-intl presente in package.json."
    - "File di test tests/i18n-routing.test.ts e tests/i18n-config.test.ts presenti ed eseguibili con vitest."
    - "Il layout src/app/[locale]/layout.tsx avvolge i children sia in NextIntlClientProvider sia in ThemeProvider (T-020), montando il tema a livello applicativo"
  acceptance_criteria:
    - id: AC-080-1
      given: "app avviata in ambiente di test"
      when: "GET /it"
      then: "risposta status 200 e l'HTML rende nel locale 'it'"
    - id: AC-080-2
      given: "app avviata in ambiente di test"
      when: "GET /es"
      then: "risposta status 200 e l'HTML rende nel locale 'es'"
    - id: AC-080-3
      given: "app avviata con localePrefix='always'"
      when: "GET / senza prefisso di locale"
      then: "risposta di redirect (307) con header Location verso /it"
    - id: AC-080-4
      given: "app avviata"
      when: "GET /fr (locale non presente in locales)"
      then: "risposta status 404"
    - id: AC-080-5
      given: "modulo src/i18n/routing.ts importato nel test"
      when: "si leggono le proprieta della configurazione"
      then: "routing.locales deep-equals ['it','es'] e routing.defaultLocale === 'it'"
    - id: AC-080-6
      given: "middleware next-intl attivo"
      when: "GET /api/health"
      then: "la risposta NON e un redirect verso /it/api/health (il matcher esclude /api e non applica il prefisso di locale)"
    - id: AC-080-7
      given: "src/app/[locale]/layout.tsx reso in un test"
      when: "si ispeziona l'albero dei provider del layout"
      then: "i children risultano avvolti sia da NextIntlClientProvider sia da ThemeProvider (tema montato a livello app, non solo testato in isolamento)"
  target_tests:
    - file: "tests/i18n-routing.test.ts"
      covers: [AC-080-1, AC-080-2, AC-080-3, AC-080-4, AC-080-6, AC-080-7]
    - file: "tests/i18n-config.test.ts"
      covers: [AC-080-5]
  security_notes:
    - "A07:2025/A02:2025 (segreti): nessun segreto hardcoded nella config i18n; solo variabili NEXT_PUBLIC_* per config non sensibile (es. brand). Nessuna chiave Supabase o service_role coinvolta qui."
    - "A01:2025 (authz): il middleware next-intl in src/middleware.ts viene esteso dalla guardia auth di T-041 (stesso unico file) senza saltarne i controlli di sessione; il matcher esclude /api ma non deve introdurre bypass di autenticazione ne toccare la RLS."
  out_of_scope:
    - "Contenuto effettivo dei cataloghi di messaggi (T-081)."
    - "Selettore di lingua e persistenza della scelta (T-082/T-083)."

- id: T-081
  title: "Cataloghi messaggi IT/ES + parita chiavi"
  macrotask: "i18n"
  depends_on: [T-080]
  objective: "Creare i cataloghi di messaggi per 'it' ed 'es' (messages/it.json, messages/es.json) con i namespace iniziali della dashboard, tipizzare le chiavi usando it.json come sorgente di verita (augmentation dei tipi next-intl), configurare il fallback al defaultLocale per chiavi mancanti e garantire con un test deterministico che l'insieme delle chiavi annidate sia identico tra le due lingue. Fornire una utility che appiattisce le chiavi annidate in path puntati per il confronto."
  definition_of_done:
    - "File messages/it.json e messages/es.json con gli stessi namespace (es. common, nav, dashboard) e stesse chiavi."
    - "File di dichiarazione tipi src/i18n/messages.d.ts che dichiara IntlMessages a partire da it.json come sorgente di verita."
    - "getRequestConfig con fallback documentato: per una chiave assente nel locale attivo si usa il valore del defaultLocale ('it')."
    - "Utility src/i18n/keys.ts che esporta flattenKeys(obj) restituente l'insieme dei path puntati delle chiavi foglia."
    - "File di test tests/i18n-parity.test.ts e tests/i18n-messages.test.ts presenti ed eseguibili con vitest."
  acceptance_criteria:
    - id: AC-081-1
      given: "messages/it.json e messages/es.json caricati"
      when: "si appiattiscono le chiavi con flattenKeys e si confrontano i due Set"
      then: "la differenza simmetrica tra i due insiemi di chiavi e vuota (Set uguali)"
    - id: AC-081-2
      given: "una chiave 'common.__test_only' aggiunta solo all'oggetto it nel test"
      when: "si esegue il confronto di parita"
      then: "il confronto identifica 'common.__test_only' come mancante in es e l'assert di uguaglianza fallisce"
    - id: AC-081-3
      given: "locale 'it' attivo"
      when: "si risolve la chiave 'common.save' dal catalogo it"
      then: "il valore restituito e esattamente 'Salva'"
    - id: AC-081-4
      given: "locale 'es' attivo"
      when: "si risolve la chiave 'common.save' dal catalogo es"
      then: "il valore restituito e esattamente 'Guardar' (diverso dal valore IT)"
    - id: AC-081-5
      given: "entrambi i cataloghi caricati"
      when: "si visita ricorsivamente ogni valore foglia"
      then: "ogni foglia e una stringa non vuota (nessun valore null, undefined o stringa vuota)"
  target_tests:
    - file: "tests/i18n-parity.test.ts"
      covers: [AC-081-1, AC-081-2, AC-081-5]
    - file: "tests/i18n-messages.test.ts"
      covers: [AC-081-3, AC-081-4]
  security_notes:
    - "A07:2025 (segreti): i cataloghi di messaggi vengono spediti al browser; non devono contenere segreti, chiavi API, token o PII, ma solo testo di UI. Nessun valore sensibile nei file messages/*.json."
  out_of_scope:
    - "Traduzione completa di tutte le stringhe dell'app (si aggiungono per feature nei rispettivi macrotask)."
    - "Selettore di lingua e persistenza (T-082/T-083)."

- id: T-082
  title: "Selettore lingua + persistenza cookie"
  macrotask: "i18n"
  depends_on: [T-081]
  objective: "Implementare un componente selettore di lingua (src/ui/LocaleSwitcher) che consente di passare tra 'it' ed 'es'. La scelta naviga verso la route localizzata corrispondente (stesso path, prefisso diverso) e persiste tramite il cookie NEXT_LOCALE letto da next-intl. Il valore di locale proveniente dal client viene validato server-side contro l'allowlist ['it','es']: un valore fuori allowlist viene rifiutato con errore e non imposta il cookie ne genera redirect. Il cookie e impostato con Path=/ e SameSite=Lax."
  definition_of_done:
    - "Componente client src/ui/LocaleSwitcher.tsx che mostra le lingue disponibili, marca quella attiva e cambia locale."
    - "Server action (o route handler) in src/domain/setLocale.ts che valida il locale contro l'allowlist ['it','es'] e imposta il cookie NEXT_LOCALE."
    - "Il cambio di locale reindirizza alla stessa pagina con il nuovo prefisso (/it <-> /es) usando l'helper di navigazione di next-intl."
    - "Il cookie NEXT_LOCALE viene emesso con attributi Path=/ e SameSite=Lax."
    - "File di test tests/i18n-locale-switch.test.ts e tests/locale-switcher.test.tsx presenti ed eseguibili con vitest."
  acceptance_criteria:
    - id: AC-082-1
      given: "utente su /it/dashboard"
      when: "seleziona 'es' nel LocaleSwitcher e invoca setLocale('es')"
      then: "viene prodotto un redirect verso /es/dashboard"
    - id: AC-082-2
      given: "invocazione di setLocale('es') valida"
      when: "si ispeziona la risposta"
      then: "e presente un header Set-Cookie per NEXT_LOCALE=es con attributi Path=/ e SameSite=Lax"
    - id: AC-082-3
      given: "richiesta di setLocale con valore 'fr' (fuori allowlist)"
      when: "l'azione viene eseguita lato server"
      then: "l'azione restituisce errore 400 e NON emette alcun Set-Cookie per NEXT_LOCALE"
    - id: AC-082-4
      given: "cookie NEXT_LOCALE=es presente nella richiesta"
      when: "GET / senza prefisso di locale"
      then: "la risposta risolve il locale 'es' (redirect/serve verso il contenuto 'es')"
    - id: AC-082-5
      given: "LocaleSwitcher renderizzato con locale attivo 'it'"
      when: "si ispeziona l'output renderizzato"
      then: "sono presenti le opzioni 'it' e 'es' e l'opzione 'it' e marcata come attiva (aria-current o attributo equivalente)"
  target_tests:
    - file: "tests/i18n-locale-switch.test.ts"
      covers: [AC-082-1, AC-082-2, AC-082-3, AC-082-4]
    - file: "tests/locale-switcher.test.tsx"
      covers: [AC-082-5]
  security_notes:
    - "Validazione input SEMPRE server-side: il valore di locale dal client e validato contro l'allowlist ['it','es'] prima di impostare cookie o costruire il redirect (A05:2025 injection): mai interpolare il valore grezzo in path o header Location (evita open-redirect e path injection)."
    - "Cookie NEXT_LOCALE con SameSite=Lax e Path=/; nessun segreto o identificatore sensibile memorizzato nel cookie."
    - "A07:2025/A02:2025 (segreti): nessun segreto hardcoded; nessuna chiave Supabase coinvolta in questo task (persistenza su DB delegata a T-083)."
  out_of_scope:
    - "Persistenza della preferenza su profiles.locale nel database (T-083)."
    - "Traduzione dei contenuti delle pagine (coperta dai rispettivi macrotask)."

- id: T-083
  title: "Persistenza locale su profiles.locale"
  macrotask: "i18n"
  depends_on: [T-082, T-061, T-005, T-041]
  objective: "Persistere la preferenza di lingua dell'utente autenticato nella colonna profiles.locale, oltre al cookie. Una server action valida il locale contro l'allowlist ['it','es'] e aggiorna profiles.locale SOLO per la riga dell'utente corrente (id = auth.uid()) usando il client Supabase server con la sessione dell'utente (RLS attiva), mai la service_role. Al bootstrap del layout, se il cookie NEXT_LOCALE e assente, profiles.locale (se impostato) determina il locale iniziale. I filtri usano metodi tipati .eq(), senza interpolazione di stringhe."
  definition_of_done:
    - "Server action src/data/updateProfileLocale.ts che aggiorna profiles.locale per l'utente autenticato tramite il client Supabase server legato alla sessione dell'utente (RLS attiva), NON service_role."
    - "Validazione server-side del valore contro ['it','es'] prima della scrittura, con errore 400 per valori fuori allowlist e 401 in assenza di utente autenticato."
    - "Il filtro di update usa .eq('id', userId) tipato; nessuna costruzione di filtri via .or()/.filter() con input interpolato."
    - "Lettura di profiles.locale al bootstrap di src/app/[locale]/layout.tsx per inizializzare il locale quando il cookie NEXT_LOCALE non e presente."
    - "Test RLS a runtime tests/profiles-locale-rls.test.ts eseguiti tramite client con auth reale su Supabase locale (supabase start), non nell'SQL editor; ogni blocco riporta il commento '// covers: <AC-id>'."
  acceptance_criteria:
    - id: AC-083-1
      given: "utente A autenticato con la propria sessione"
      when: "invoca updateProfileLocale('es')"
      then: "una select di A su profiles restituisce locale === 'es' per la riga id = A"
    - id: AC-083-2
      given: "utente A autenticato"
      when: "tenta di aggiornare profiles.locale della riga dell'utente B"
      then: "l'update colpisce 0 righe (RLS lo blocca) e profiles.locale di B resta invariato"
    - id: AC-083-3
      given: "utente A autenticato"
      when: "invoca updateProfileLocale('de') (fuori allowlist)"
      then: "l'azione restituisce errore 400 e nessuna scrittura avviene su profiles (locale di A invariato)"
    - id: AC-083-4
      given: "nessun utente autenticato (nessuna sessione)"
      when: "invoca updateProfileLocale('es')"
      then: "l'azione restituisce errore 401 e nessuna riga di profiles viene modificata"
    - id: AC-083-5
      given: "profiles.locale = 'es' per l'utente A e nessun cookie NEXT_LOCALE nella richiesta"
      when: "A carica la dashboard"
      then: "il layout risolve il locale 'es' e la UI rende in 'es'"
  target_tests:
    - file: "tests/profiles-locale-rls.test.ts"
      covers: [AC-083-1, AC-083-2, AC-083-4]
    - file: "tests/profiles-locale-action.test.ts"
      covers: [AC-083-3, AC-083-5]
  security_notes:
    - "A01:2025 (broken access control) / RLS: la scrittura passa dal client Supabase con RLS attiva; R4 la policy UPDATE su profiles e vincolata a id = (select auth.uid()); R1/R2 RLS abilitata con almeno una policy su profiles; R3 nessuna condizione USING(true)/WITH CHECK(true)."
    - "R7: la service_role NON viene usata per questa scrittura (nessun bypass RLS lato server); se un handler usasse mai la service_role, farebbe authz esplicita, ma qui si usa il client con sessione utente."
    - "R6: la policy UPDATE su profiles e accompagnata dalla policy SELECT (definite in T-061), necessaria per la verifica del risultato."
    - "R9: uso di (select auth.uid()) nelle policy per il caching del planner; la colonna id (PK) e indicizzata."
    - "A05:2025 (injection): filtri con metodi tipati .eq('id', userId); vietata la PostgREST filter injection via .or()/.filter() con input interpolato. Validazione input server-side del locale contro l'allowlist ['it','es'] (mai fidarsi del client per l'identita o il valore)."
    - "A07:2025/A02:2025 (segreti): chiavi Supabase via env; anon key solo nel browser, service_role solo server-side e non usata in questo task."
  out_of_scope:
    - "Modifica dello schema di profiles (colonna locale gia definita dal contratto di naming e creata a monte in T-061)."
    - "Selettore UI e cookie (T-082)."
```

## Self-check

- **Strutturale** (deterministico): `validate_blueprint.mjs` sulla dir del blueprint — atteso exit 0.
- **Semantico** (checklist guidata): punti 6–10 su ogni task; i rilievi vanno all'human-in-the-loop.
